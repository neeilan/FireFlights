<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cs.b07.cscb07courseproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/airplane_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignInActivity" />
        <activity android:name=".BillingInfoActivity" />
        <activity
            android:name=".FindFlightsActivity"
            android:label="@string/title_activity_find_flights"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UploadClientsActivity"
            android:label="@string/title_activity_upload_clients"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".UploadFlightsActivity" />
        <activity android:name=".BookItineraryActivity" />
        <activity android:name=".UserItinerariesActivity" />
        <activity android:name=".ViewClientsActivity" />
        <activity android:name=".ViewFlightsActivity" />
        <activity android:name=".FlightInfoActivity" />
        <activity
            android:name=".SearchActivity"
            android:label="@string/title_activity_search"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddNewFlightActivity" />
        <activity android:name=".AddNewClientActivity" />
        <activity android:name=".HomeActivity"></activity>
    </application>

</manifest>